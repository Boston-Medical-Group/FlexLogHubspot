{
  "name": "flex-log-hubspot-serverless",
  "version": "0.0.3",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.3.0",
    "node-fetch": "^2.6.7",
    "twilio": "^3.79.0",
    "twilio-flex-token-validator": "^1.5.6"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.9",
    "copyfiles": "^2.2.0",
    "nodemon": "^2.0.20",
    "twilio-run": "^3.5.3",
    "wrap-ansi": "^9.0.0"
  },
  "nodemonConfig": {
    "ext": "ts",
    "watch": [
      "src"
    ]
  },
  "engines": {
    "node": "16"
  }
}
